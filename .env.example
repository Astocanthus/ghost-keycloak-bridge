# --- SERVER CONFIG ---
PORT=3000
NODE_ENV=production

# --- URLs ---
# The public URL of your Ghost blog
BLOG_PUBLIC_URL=https://blog.example.com
# The root domain for cookie sharing (must start with a dot)
COOKIE_DOMAIN=.example.com
# The callback URL pointing to this bridge
CALLBACK_URL=https://blog.example.com/auth/callback
# Internal URL to reach Ghost container (usually http://ghost-container-name:2368)
GHOST_INTERNAL_URL=http://ghost:2368

# --- KEYCLOAK OIDC ---
KEYCLOAK_ISSUER=https://auth.example.com/realms/myrealm
KEYCLOAK_ID=ghost-bridge
KEYCLOAK_SECRET=your_keycloak_client_secret_here

# --- GHOST INTEGRATION ---
# Admin API Key from Ghost Settings > Integrations > Custom
GHOST_ADMIN_API_KEY=1234567890abcdef:abcdef1234567890

# Private Key from Ghost Database (table: settings, key: members_private_key)
# Format must include -----BEGIN RSA PRIVATE KEY-----
# Use \n for newlines if passing via docker env, or keep multiline in .env file
GHOST_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIICXAIBAAKBgQC...\n-----END RSA PRIVATE KEY-----"